// Here we create a header component, that is a function that returns a header.
// no props when we define the function, because we are not passing any data to it.
export const Header = () => {
  return (
    <header>
      <h1>Fresh, brand new, latest music!</h1>
    </header>
  );
}