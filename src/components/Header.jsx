export const Header = () => {
    const headerText = "New Albums & Singles"
    
    return ( 
        <div className="header-wrapper"> 
            <header>
                <h1>{headerText}</h1>
            </header>
        </div>
    );
};